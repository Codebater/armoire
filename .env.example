# Optional — enables AI tagging of uploaded pieces (Claude vision).
# Without a key the app still works fully: colors are detected locally
# and you confirm category/tags in the review step.
ANTHROPIC_API_KEY=

# Optional model override (default: claude-haiku-4-5)
ANTHROPIC_MODEL=
